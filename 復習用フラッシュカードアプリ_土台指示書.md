# 復習用フラッシュカードアプリの土台指示書

授業ごとの復習アプリを、誰でも追加・編集できる形で作るための標準仕様です。新規作成・既存アプリ改修のどちらにも使います。

## 目的

- 授業単位でフラッシュカードと四択クイズを復習できる、スマホ優先の公開Webアプリにする。
- 生徒・講師が、アカウント不要で授業、問題、解説、画像、資料リンクを追加・編集・削除・並び替えできるようにする。
- 学習の途中状態と解き直し対象は端末内に保存し、教材そのものは共有データベースに保存する。

## 必須画面と体験

1. **メニュー**
   - 授業を日付の新しい順に、コンパクトな一行カードで表示する。
   - 授業カードを押すと、開始・解き直し・問題一覧・動画/資料リンクを展開する。
   - タイトル横にはYouTube、画像、資料をそれぞれアイコンボタンで置く。
   - 「問題一覧」と「ノートを作る・編集する」を用意し、説明文で画面を縦に伸ばさない。

2. **フラッシュカード**
   - 問題面と解説面は一度に見せず、`答えを見る`（Space/Enterも対応）で反転する。
   - 解説表示後だけ、`わかった` と `解き直しに追加` を操作可能にする。
   - 問題・解説とも画像を表示でき、解説中のURLは短いリンクボタンとして描画する。元URLは編集時に保持する。
   - 途中で前後移動、メニュー帰還、途中再開ができる。
   - 進捗、経過時間、結果の定着ランクを表示する。時間は採点に使わない。

3. **四択クイズ（必要な授業だけ）**
   - 選択直後に正誤と解説を表示する。
   - 解き直し追加、前後移動、メニュー帰還、途中再開、問題一覧への遷移を実装する。

4. **問題一覧**
   - 問題・解説・画像・解き直し状態を閲覧できる。
   - 各カードをドラッグ＆ドロップで並び替え可能にする。
   - 順番に応じて表示番号を常に `Q1` から振り直す。内部IDは変えず、進捗や解き直しを壊さない。
   - 問題カードから直接編集画面へ行けるようにする。

5. **共有編集画面**
   - 授業の追加・編集・削除・並び替え、カードの追加・編集・削除・復元・並び替えを提供する。
   - 問題、答え/解説、四択選択肢、正解、画像URLまたはドラッグ＆ドロップ画像を編集可能にする。
   - 削除は復元できる論理削除にする。表示番号は隙間なく振り直す。
   - 公開共有編集を要件とする場合は、管理ログイン不要にする。制限が必要なら認証方式を先に決める。

## データ設計

- `Lesson`: `id`, `date`, `title`, `type`, `videoUrl`, `resources`, `sortOrder`, `deleted`
- `Flashcard`: `id`, `lessonId`, `question`, `answer`, `imageUrl`, `deleted`
- `QuizQuestion`: `id`, `lessonId`, `question`, `choices[4]`, `correctIndex`, `explanation`, `imageUrl`, `deleted`
- `LessonResource`: `lessonId`, `label`, `url`, `kind` (`youtube | image | document | link`)
- `CardOrder`: `lessonId`, `cardId`, `sortOrder`
- 共有教材はD1等のサーバーDB、画像はR2等のオブジェクトストレージ、学習状態は `localStorage` に分離する。

## 編集・画像・リンクの規約

- ドラッグした画像はアップロードし、生成URLを問題または解説に差し込む。貼り付けURLも許可する。
- URLの表示は自動短縮する。
  - YouTube: `YouTubeで見る`
  - Google Docs等: サービス名付きボタン
  - 画像: 画像アイコン
  - その他: `ドメイン名を開く`
- 編集欄には短縮前のURL文字列を残す。
- 麻雀牌を画像化する場合は、対象プロジェクトの承認済み牌素材を使う。

## 実装ルール

- 表示順とIDを混同しない。並び替えは `sortOrder` だけを変更する。
- 楽観更新後、失敗時は画面とデータを元へ戻す。
- ブラウザ保存に失敗しても、そのセッションだけで学習継続できるようにする。
- 追加・編集・削除・並び替えは、更新後に再読み込みして共有データの反映を確認する。
- 画面文言、タイトル、日付、問題数はハードコードのまま放置せず、編集データから描画する。

## 品質確認と公開

- カード反転前に自己判定できないこと、キーボード操作、途中再開、解き直し追加/解除、削除復元、D&D並び替え、画像D&Dをブラウザで確認する。
- 追加・削除・並び替え後、表示番号が `Q1...Q件数` で連続していることを確認する。
- 表示版と内部 `APP_VERSION` を同時に上げ、型検査・自動テスト・ビルド・差分検査を実行する。
- 公開前にGitHubへコミット/プッシュし、本番へデプロイ後、公開URLと主要API/画像を確認する。

## 依頼時に最初に確認すること

- 対象リポジトリと公開URL
- 授業タイトル・日付・動画URL・資料URL
- フラッシュカードか四択か（併用可）
- 誰が編集できるか（公開編集または認証）
- 既存データやデザインを維持する範囲
